# Programming 수업 소스코드
## 02_counter
vanila HTML, JavaScript
## React 시작
```shell
npm create vite@latest .
```

### CounterApp
- `useState()`
- `onClick={() => set함수()}`
- `onclick={() => set함수((이전State)=>이전state+1)}`