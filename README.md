# Programming 수업 소스코드
## 02_counter
vanila HTML, JavaScript
## React 시작
```shell
npm create vite@latest .
```

### CounterApp
- `useState()` 
- `onClick={() => set함수()}`
- `onclick={() => set함수((이전State)=>이전state+1)}`
### TodoListApp
- React Component 분리
- for -> htmlFor, class -> className
- props
- `<input id={id} value={} />`, `<label htmlFor={id}/>`
- onCange, onClick, onKeyUp
- 구조 분해 할당, `...스프레드 연산자`
- `<form ouSubmit {}></form>`
- `map()`: RU, `fillter()` : D
- `<TodoIten key={}/>`
- `const handleEvent = (event)=> {}` 
- `(조건식 ? 참 : 거짓)`
- `(조건식 && 참)`
- `(조건식 && 거짓)`
- LocalStorege, `useEffect()`
